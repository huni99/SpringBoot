<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- namespace의 이름은 사용하려는 DAO의 풀패키지명과 동일하게 작성 -->
<mapper namespace="com.winter.app.qna.QnaDAO">
	<resultMap type="QnaVO" id="Qnadetail">
		<id property="boardNum" column="BOARD_NUM"/>
		<result property="boardTitle" column="BOARD_TITLE"/>
		<result property="boardContents" column="BOARD_CONTENTS"/>
		<result property="boardWriter" column="BOARD_WRITER"/>
		<result property="boardDate" column="BOARD_DATE"/>
		<result property="boardHit" column="BOARD_HIT"/>
		<result property="boardRef" column="BOARD_REF"/>
		<result property="boardStep" column="BOARD_STEP"/>
		<result property="boardDepth" column="BOARD_DEPTH"/>
		<collection property="boardFileVOs" javaType="java.util.List" ofType="BoardFileVO">
			<id property="fileNum" column="file_num"/>
			<result property="oriName" column="ori_name"/>
			<result property="saveName" column="save_name"/>
		</collection>
	</resultMap>
	<select id="list" resultType='QnaVO' parameterType="Pager">
		SELECT * FROM QNA 
		ORDER BY board_ref DESC, board_step ASC 
		limit #{startIndex}, #{endIndex}
	
	</select>
	<insert id="insert" parameterType="QnaVO" useGeneratedKeys="true" keyProperty="boardNum">
		INSERT INTO QNA (BOARD_NUM ,BOARD_TITLE,
		BOARD_CONTENTS, BOARD_WRITER ,
		BOARD_DATE, BOARD_HIT,BOARD_REF , BOARD_STEP,BOARD_DEPTH)
		VALUES
		(NULL,#{boardTitle},#{boardContents},#{boardWriter},now(),0,0,0,0)
	
	</insert>
	<update id="refUpdate" parameterType="QnaVO">
		UPDATE QNA SET BOARD_REF=#{boardNum} WHERE BOARD_NUM=#{boardNum}
	</update>
	<select id="detail" parameterType="QnaVO" resultMap="Qnadetail">
		SELECT * FROM QNA q LEFT OUTER JOIN QNAFILES f on q.board_num=f.board_num
		WHERE q.BOARD_NUM=#{boardNum}
	</select>
	<update id="replyUpdate" parameterType="QnaVO">
		UPDATE QNA SET BOARD_STEP= BOARD_STEP +1
		WHERE BOARD_REF=#{boardRef} AND BOARD_STEP > #{boardStep}

	</update>
	<insert id="replyInsert" parameterType="QnaVO" useGeneratedKeys="true" keyProperty="boardNum">
		INSERT INTO QNA (BOARD_NUM ,BOARD_TITLE,
		BOARD_CONTENTS, BOARD_WRITER ,
		BOARD_DATE, BOARD_HIT,BOARD_REF , BOARD_STEP,BOARD_DEPTH)
		VALUES
		(NULL,#{boardTitle},#{boardContents},#{boardWriter},now(),0,#{boardRef},#{boardStep},#{boardDepth})
		
		
	</insert>
	<insert id="insertFile" parameterType="BoardFileVO">
		INSERT INTO QNAFILES
		VALUES (null, #{oriName},#{saveName},#{boardNum})
	</insert>
	<select id="totalCount" parameterType="QnaVO">
		SELECT COUNT(board_num) FROM QNA
	
	</select>
	<update id="update" parameterType="QnaVO">
		UPDATE QNA
		SET
		BOARD_TITLE=#{boardTitle} , BOARD_CONTENTS= #{boardContents},
		BOARD_WRITER=#{boardWriter}
		WHERE BOARD_NUM = #{boardNum}
	</update>
	<select id="fileDetail" parameterType="BoardFileVO" resultType="BoardFileVO">
		SELECT * FROM qnafiles
		WHERE file_num=#{fileNum}
	
	</select>
	<delete id="fileDeleteOne" parameterType="BoardFileVO">
		DELETE FROM QNAFILES
		WHERE file_num=#{fileNum}
	</delete>
	<delete id= "delete" parameterType="QnaVO">
		UPDATE qna SET board_title = " "
		WHERE board_num=#{boardNum}
	</delete>
</mapper>